

// eslint-disable-next-line react/prop-types
const CompoWrapper = ({children}) => {
    return (
        <div className="mx-4">
            {children}
        </div>
    );
};

export default CompoWrapper;